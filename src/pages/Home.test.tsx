import { render, screen } from "@testing-library/react"
import React from "react"
import { TransactionForm } from "../components/TransactionForm"

describe('Home page', () => {
    it('should allow user to add transaction', () => {
        expect(true).toBe(true)
    })
})